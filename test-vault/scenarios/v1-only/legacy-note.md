# Legacy v1 note

Anchors are `^<13-digit>` (v1 used `Date.now()`); there is no `flashcards:`
frontmatter map. First sync should detect these and offer the migration modal.

What is the capital of Spain?::Madrid ^1700000000101

What is the largest planet?::Jupiter ^1700000000102
