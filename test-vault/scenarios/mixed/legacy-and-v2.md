---
flashcards:
  q-cccc: { nid: 1700000000201, hash: qrstuvwx }
---

# Mixed note: one v1 anchor + one v2 anchor

The v1 anchor is unmigrated; the v2 anchor is already in the frontmatter map.
First sync should detect the v1 anchor and trigger the migration modal with
count = 1.

Question already migrated to v2?::Yes ^q-cccc

Question still on v1?::Pending migration ^1700000000202
