# Notes with no flashcards

This file has prose but zero flashcard syntax. Sync should skip it silently
without writing anything (no anchors, no frontmatter).

Plain markdown content here. No `::`, no `==cloze==`, no `#card` tags.
